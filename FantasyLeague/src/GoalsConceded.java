
public interface GoalsConceded {
    GameWeekBufferedEditor gwbe = new GameWeekBufferedEditor();
    public void concedeGoals(String playerName);
    public void concedeGoals(String playerName, int time);
}
